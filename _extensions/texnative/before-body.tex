$if(has-frontmatter)$
  \frontmatter
$endif$

$if(title)$

  $if(beamer)$
    \frame{\titlepage}
  $else$
    $page-title.tex()$
  $endif$

  $if(abstract)$
    \begin{abstract}
      $abstract$
    \end{abstract}
  $endif$

$endif$
